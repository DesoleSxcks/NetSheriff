# Database (SQLite file path)
DATABASE_URL="file:./data/database.sqlite"

# Application port
PORT=3000

# Log level: error, warn, info, verbose, debug, silly
LOG_LEVEL=info

# JWT secret (set when adding authentication)
JWT_SECRET=your_jwt_secret_here
